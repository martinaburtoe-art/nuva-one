project_id = "vnzyecnbdqbfuxawzrda"

[auth]
# Security baseline mirrored in local CI. Hosted Auth settings must match these values.
password_hibp_enabled = true
password_min_length = 12
password_required_characters = "lower_upper_letters_digits_symbols"
sessions_timebox = 12
sessions_inactivity_timeout = 0.5
sessions_single_per_user = false
refresh_token_rotation_enabled = true
