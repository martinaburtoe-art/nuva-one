name: Legacy homepage showcase integration (manual only)

on:
  workflow_dispatch:

# Deprecated: homepage composition is now maintained directly by the application
# and the dedicated public-homepage finalization workflow.
# This workflow is intentionally manual to prevent stale automated rewrites.
jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy homepage integration workflow is disabled; no changes applied."
