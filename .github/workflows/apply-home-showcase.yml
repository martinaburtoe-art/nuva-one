name: Finalize public homepage integration

on:
  workflow_dispatch:

# Deprecated: homepage composition is maintained directly by the application.
# Manual-only placeholder retained for traceability; it never writes to main.
permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy homepage finalization workflow is disabled; no changes applied."
