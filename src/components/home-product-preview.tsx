import { NuvaScrollWorld } from "@/components/nuva-scroll-world";

/**
 * Homepage product experience.
 * The historical rotating module preview is now evolved into the cinematic
 * Scroll World while keeping the existing import/API used by the homepage.
 */
export function HomeProductPreview() {
  return <NuvaScrollWorld />;
}
