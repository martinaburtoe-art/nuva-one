import { useEffect, useMemo, useState } from "react";
import { ExternalLink, RefreshCw, Sparkles, Landmark, Scale, WalletCards, BellRing, ShieldAlert, CircleCheck, Info, Target } from "lucide-react";
import { Card } from "@/components/ui/card";
import { Button } from "@/components/ui/button";
import { Badge } from "@/components/ui/badge";

type RadarType = "Oportunidad" | "Atención" | "Información";
type NewsItem = { title: string; link: string; pubDate?: string; source: string; category: string; summary?: string; type: RadarType; relevance: number };
const FEEDS = [
  { url: "https://www.sii.cl/rss/noticias.xml", source: "SII", category: "Tributario" },
  { url: "https://www.sercotec.cl/feed/", source: "Sercotec", category: "Fondos y apoyo" },
  { url: "https://www.corfo.cl/sites/cpp/sala_prensa", source: "Corfo", category: "Financiamiento e innovación" },
];
const FALLBACK: NewsItem[] = [
  { title: "Sercotec: nuevas convocatorias y programas de apoyo para pymes", link: "https://www.sercotec.cl/noticias/", source: "Sercotec", category: "Fondos y apoyo", summary: "Revisa convocatorias, subsidios, capacitaciones y programas disponibles para micro y pequeñas empresas.", type: "Oportunidad", relevance: 94 },
  { title: "SII: novedades tributarias y obligaciones para contribuyentes", link: "https://www.sii.cl/noticias/", source: "SII", category: "Tributario", summary: "Consulta las últimas novedades, instrucciones y comunicaciones oficiales del Servicio de Impuestos Internos.", type: "Atención", relevance: 92 },
  { title: "Corfo: oportunidades para innovación, digitalización y crecimiento", link: "https://www.corfo.cl/sites/cpp/homecorfo", source: "Corfo", category: "Financiamiento e innovación", summary: "Explora instrumentos y programas para innovación, productividad y transformación empresarial.", type: "Oportunidad", relevance: 89 },
];
const OPPORTUNITY_WORDS = ["convocatoria", "fondo", "subsidio", "capital semilla", "capital abeja", "postul", "financiamiento", "programa", "beneficio", "innovación", "digitalización", "concurso"];
const ATTENTION_WORDS = ["ley", "obligación", "tribut", "iva", "plazo", "vencimiento", "resolución", "normativa", "fiscalización", "declaración", "sii"];
function cleanSummary(value: unknown) { return String(value ?? "").replace(/<[^>]*>/g, "").replace(/\s+/g, " ").trim().slice(0, 220); }
function classify(title: string, source: string): { type: RadarType; relevance: number } {
  const text = `${title} ${source}`.toLowerCase();
  const opportunity = OPPORTUNITY_WORDS.reduce((score, word) => score + (text.includes(word) ? 9 : 0), 0);
  const attention = ATTENTION_WORDS.reduce((score, word) => score + (text.includes(word) ? 10 : 0), 0);
  if (opportunity >= attention && opportunity >= 18) return { type: "Oportunidad", relevance: Math.min(98, 68 + opportunity) };
  if (attention >= 18) return { type: "Atención", relevance: Math.min(98, 68 + attention) };
  return { type: "Información", relevance: Math.min(88, 62 + Math.max(opportunity, attention)) };
}
export function PymeNewsHub() {
  const [items, setItems] = useState<NewsItem[]>(FALLBACK); const [loading, setLoading] = useState(false); const [updatedAt, setUpdatedAt] = useState<Date | null>(null); const [filter, setFilter] = useState("Todas");
  const load = async () => { setLoading(true); const collected: NewsItem[] = []; await Promise.all(FEEDS.map(async (feed) => { try { const endpoint = `https://api.rss2json.com/v1/api.json?rss_url=${encodeURIComponent(feed.url)}`; const res = await fetch(endpoint, { cache: "no-store" }); if (!res.ok) return; const data = await res.json(); (data.items ?? []).slice(0, 7).forEach((item: { title?: string; link?: string; pubDate?: string; description?: string }) => { if (!item.title || !item.link) return; collected.push({ title: item.title, link: item.link, pubDate: item.pubDate, source: feed.source, category: feed.category, summary: cleanSummary(item.description), ...classify(item.title, feed.source) }); }); } catch {} })); const unique = Array.from(new Map(collected.map((item) => [item.link, item])).values()); if (unique.length) setItems(unique.sort((a, b) => b.relevance - a.relevance || new Date(b.pubDate ?? 0).getTime() - new Date(a.pubDate ?? 0).getTime()).slice(0, 12)); setUpdatedAt(new Date()); setLoading(false); };
  useEffect(() => { void load(); const id = window.setInterval(() => void load(), 30 * 60 * 1000); return () => window.clearInterval(id); }, []);
  const categories = useMemo(() => ["Todas", ...Array.from(new Set(items.map((i) => i.category)))], [items]); const visible = filter === "Todas" ? items : items.filter((i) => i.category === filter); const priority = items[0]; const opportunityCount = items.filter((i) => i.type === "Oportunidad").length; const attentionCount = items.filter((i) => i.type === "Atención").length;
  return <Card className="overflow-hidden border-primary/15 bg-gradient-to-br from-primary/[0.05] via-background to-background"><div className="border-b p-5 md:p-6"><div className="flex flex-wrap items-start justify-between gap-4"><div className="flex items-start gap-3"><div className="rounded-xl bg-primary/10 p-2 text-primary"><BellRing className="h-5 w-5" /></div><div><div className="flex flex-wrap items-center gap-2"><p className="text-xs font-semibold uppercase tracking-[0.16em] text-primary">Nüva PYME Radar</p><Badge variant="secondary" className="gap-1 text-[10px]"><Sparkles className="h-3 w-3" /> Inteligencia externa</Badge></div><h2 className="mt-1 text-lg font-semibold">Oportunidades y riesgos que pueden mover tu negocio</h2><p className="mt-1 max-w-2xl text-sm text-muted-foreground">Nüva monitorea fuentes públicas y prioriza fondos, cambios tributarios, normativa y programas relevantes para PYMEs chilenas.</p></div></div><Button variant="outline" size="sm" onClick={() => void load()} disabled={loading}><RefreshCw className={`mr-2 h-3.5 w-3.5 ${loading ? "animate-spin" : ""}`} />Actualizar</Button></div><div className="mt-5 grid gap-3 sm:grid-cols-3"><div className="rounded-xl border bg-background/70 p-3"><p className="text-[11px] text-muted-foreground">Señal principal</p><p className="mt-1 text-sm font-semibold">{priority?.title ?? "Sin novedades"}</p><p className="mt-1 text-[11px] text-muted-foreground">Relevancia {priority?.relevance ?? 0}/100</p></div><div className="rounded-xl border bg-background/70 p-3"><p className="text-[11px] text-muted-foreground">Oportunidades</p><p className="mt-1 text-xl font-bold">{opportunityCount}</p><p className="text-[11px] text-muted-foreground">Fondos, convocatorias y programas</p></div><div className="rounded-xl border bg-background/70 p-3"><p className="text-[11px] text-muted-foreground">Atención</p><p className="mt-1 text-xl font-bold">{attentionCount}</p><p className="text-[11px] text-muted-foreground">Cambios y obligaciones a revisar</p></div></div>{priority && <div className="mt-4 flex flex-wrap items-center justify-between gap-3 rounded-xl border border-primary/15 bg-primary/[0.04] p-3"><div className="flex min-w-0 items-center gap-2"><Target className="h-4 w-4 shrink-0 text-primary" /><div className="min-w-0"><p className="text-[11px] font-semibold uppercase tracking-wide text-primary">Priority Signal</p><p className="truncate text-sm font-medium">{priority.title}</p></div></div><a href={priority.link} target="_blank" rel="noreferrer" className="inline-flex shrink-0 items-center text-xs font-semibold text-primary">Revisar fuente <ExternalLink className="ml-1 h-3 w-3" /></a></div>}<div className="mt-4 flex flex-wrap gap-2">{categories.map((c) => <Button key={c} size="sm" variant={filter === c ? "default" : "outline"} onClick={() => setFilter(c)}>{c}</Button>)}</div></div><div className="grid gap-3 p-5 md:grid-cols-2 md:p-6 lg:grid-cols-3">{visible.map((item, idx) => { const Icon = item.source === "SII" ? Scale : item.source === "Corfo" ? Landmark : WalletCards; const TypeIcon = item.type === "Oportunidad" ? CircleCheck : item.type === "Atención" ? ShieldAlert : Info; return <article key={`${item.link}-${idx}`} className="group rounded-xl border bg-background/75 p-4 transition-all hover:-translate-y-0.5 hover:border-primary/30 hover:shadow-soft"><div className="flex items-center justify-between gap-2"><Badge variant="outline" className="gap-1"><Icon className="h-3 w-3" />{item.source}</Badge><Badge variant={item.type === "Atención" ? "destructive" : "secondary"} className="gap-1 text-[10px]"><TypeIcon className="h-3 w-3" />{item.type}</Badge></div><div className="mt-2 flex items-center justify-between gap-2"><span className="text-[10px] text-muted-foreground">{item.category}</span><span className="text-[10px] font-medium text-primary">{item.relevance}/100</span></div><h3 className="mt-2 line-clamp-3 text-sm font-semibold leading-5">{item.title}</h3>{item.summary && <p className="mt-2 line-clamp-3 text-xs leading-5 text-muted-foreground">{item.summary}</p>}<div className="mt-4 flex items-center justify-between gap-2"><span className="text-[10px] text-muted-foreground">{item.pubDate ? new Date(item.pubDate).toLocaleDateString("es-CL") : "Actual"}</span><a href={item.link} target="_blank" rel="noreferrer" className="inline-flex items-center text-xs font-semibold text-primary">Fuente oficial <ExternalLink className="ml-1 h-3 w-3" /></a></div></article>; })}</div><div className="flex flex-wrap items-center justify-between gap-2 border-t bg-muted/20 px-5 py-3 text-[11px] text-muted-foreground md:px-6"><span>{updatedAt ? `Última actualización ${updatedAt.toLocaleTimeString("es-CL", { hour: "2-digit", minute: "2-digit" })}` : "Actualizando fuentes..."} · actualización automática cada 30 min</span><span>Fuentes públicas/oficiales · verificar siempre condiciones y plazos en la fuente original.</span></div></Card>;
}
